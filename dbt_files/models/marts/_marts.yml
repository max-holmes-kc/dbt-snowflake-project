version: 2

models:
  - name: fact_policy
    description: Fact table combining policy data from all source systems
    columns:
{{ generate_fact_policy_columns() }}
      - name: source_system
        description: Identifies the source system (circles or s_insurance)
        tests:
          - not_null
          - accepted_values:
              values: ['circles', 's_insurance']
