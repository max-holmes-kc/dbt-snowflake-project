version: 2

seeds:      
  - name: circles_policies
    description: Raw policy data from circles system
    config:
      schema: RAW
      
  - name: s_insurance_policies
    description: Raw policy data from s_insurance system
    config:
      schema: RAW

  - name: policy_mapping
    description: Policy mapping data
    config:
      schema: RAW